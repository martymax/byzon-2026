import type { RequestHandler } from 'msw';

/**
 * Feature slices append handlers here together with their exact contract and
 * validated fixture. F0-05 deliberately invents no feature DTO or behavior.
 */
export const mockHandlers: readonly RequestHandler[] = Object.freeze([]);
